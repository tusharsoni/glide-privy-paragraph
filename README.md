# Glide + Privy Demo

Minimal React app demonstrating Glide payments with a Privy embedded wallet.

## Setup

```bash
bun install
cp .env.example .env
```

Fill in `.env`:

```
VITE_PRIVY_APP_ID=your_privy_app_id
VITE_GLIDE_PROJECT_ID=your_glide_project_id
```

## Run

```bash
bun dev
```
